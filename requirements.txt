# Pinned to the exact versions this hub is tested against (see
# security_best_practices_report.md, SEC-003). Bump deliberately, re-run
# `python -m pytest -q`, and update these lines together — do not widen back
# to an open lower bound.
flask==3.0.3
requests==2.34.2
# WebP re-encoding of generated images (app.py _to_webp_b64). Optional at
# runtime -- the hub falls back to the generator's own format if it is
# missing -- but pinned here so every install actually gets WebP.
Pillow==10.2.0
